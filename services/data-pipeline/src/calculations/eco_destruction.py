"""
eco_destruction.py — future home of computeEcoDestruction().

This is a placeholder. See the TypeScript version for the full implementation:
  apps/web/app/components/FoodTable/FoodTableCalculations.ts → computeEcoDestruction()

Migration plan:
  1. Move the function + biological constants here
  2. Call from lib/insert_foods_normalized.py at build time
  3. Store eco_destruction_score + eco_destruction_detail in foods_normalized
  4. Update schema-normalized.sql via the db-schema agent
  5. Remove from the web app's FoodTableCalculations.ts
"""
