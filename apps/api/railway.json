{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile",
    "watchPatterns": ["apps/api/**", "data/**", "services/data-pipeline/**"]
  },
  "deploy": {
    "healthcheckPath": "/api/foods",
    "restartPolicyType": "ON_FAILURE"
  }
}
