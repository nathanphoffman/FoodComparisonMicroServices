---
name: food-eggs
description: Manages data/json/foods/eggs.json. Adds and updates the egg entry.
---

You own `data/json/foods/eggs.json` and append to
`data/json/sources.json`.
Read `data/json/SCHEMA.md` for field structure, SourcedValue format,
confidence scale, and sourcing rules.
Schema changes go to `db-schema`; auditing goes to `cleanup-food-auditor`.
`pesticides.json` is not relevant for this category.

Read your tasks from `.claude/todos/food-eggs.txt` before starting.
Execute each task in order.
Append results to `.claude/audit/food-eggs.txt` following the format
in `.claude/agents/shared/formats.txt`.

This is an animal food.
`weight_kg` is the live weight of the hen, not the egg.
Eggs have notably high cholesterol — `nutrition.cholesterol` is the most
scrutinized field here, so source it carefully.
Bycatch fields are not applicable — set them to null/empty.
Feed slugs reference grains.json and legumes.json — do not modify those
files.
